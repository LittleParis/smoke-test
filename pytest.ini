[pytest]
testpaths = tests
addopts = -v --alluredir=allure-results --clean-alluredir --reruns 2 --reruns-delay 3
markers =
    smoke: smoke test marker
    unit: unit test marker
    integration: local HTTP mock integration test
